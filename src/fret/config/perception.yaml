perception:
  # Update frequency for /obstacle_cloud [Hz]
  update_hz: 1.0

  # Uniform surface-sampling density [points/m^2]
  surface_density: 2000.0

  # Obstacles for perception bridge demos. OpenMANIPULATOR-X tabletop
  # clutter is defined in the OM-X scenario / MJCF (AWS desk props).
  obstacles: []
