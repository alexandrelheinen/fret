# Dubins vehicle + Pure Pursuit parameters (SC-v11 / T11-04).
# Values aligned with ARCO simulator VehicleConfig defaults.

/**:
  ros__parameters:
    max_speed: 7.0
    min_speed: 0.0
    cruise_speed: 3.6
    lookahead_distance: 3.5
    goal_radius: 0.75
    max_turn_rate_deg: 75.0
    max_acceleration: 4.0
    max_turn_rate_dot_deg: 300.0
    curvature_gain: 0.6
    repulsion_gain: 3.0
    update_rate: 20.0
