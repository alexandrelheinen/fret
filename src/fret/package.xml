<?xml version="1.0"?>
<?xml-model href="http://download.ros.org/schema/package_format3.xsd" schematypens="http://www.w3.org/2001/XMLSchema"?>
<package format="3">
  <name>fret</name>
  <version>1.0.0</version>
  <description>
    Full-stack Robotic Effector Trajectories (FRET) — a ROS 2 workspace for
    C-space manipulation planning and Jacobian-based trajectory tracking.
  </description>
  <maintainer email="maintainer@example.com">FRET Maintainers</maintainer>
  <license>Apache-2.0</license>

  <!-- Build dependencies -->
  <buildtool_depend>ament_cmake</buildtool_depend>
  <buildtool_depend>ament_cmake_python</buildtool_depend>
  <build_depend>xacro</build_depend>

  <!-- Runtime dependencies -->
  <depend>rclpy</depend>
  <depend>sensor_msgs</depend>
  <depend>geometry_msgs</depend>
  <depend>std_msgs</depend>
  <depend>trajectory_msgs</depend>
  <depend>tf2_ros</depend>
  <depend>tf2_geometry_msgs</depend>

  <!-- Launch infrastructure -->
  <depend>launch</depend>
  <depend>launch_ros</depend>

  <!-- Visualisation and simulation -->
  <depend>robot_state_publisher</depend>
  <depend>joint_state_publisher</depend>
  <depend>rviz2</depend>
  <depend>ros_gz_sim</depend>
  <depend>ros_gz_bridge</depend>

  <!-- ros2_control for Gazebo Harmonic SITL joint control -->
  <depend>gz_ros2_control</depend>
  <depend>ros2_control</depend>
  <depend>ros2_controllers</depend>
  <depend>controller_manager</depend>
  <depend>joint_state_broadcaster</depend>
  <depend>velocity_controllers</depend>

  <!-- Action interface generation -->
  <build_depend>rosidl_default_generators</build_depend>
  <exec_depend>rosidl_default_runtime</exec_depend>
  <member_of_group>rosidl_interface_packages</member_of_group>

  <!-- Test dependencies -->
  <test_depend>ament_lint_auto</test_depend>
  <test_depend>launch_testing</test_depend>
  <test_depend>launch_testing_ros</test_depend>

  <export>
    <build_type>ament_cmake</build_type>
    <!-- Expose share/<pkg>/ as a Gazebo Harmonic resource path so that
         model://fret/meshes/... URIs resolve correctly when spawning via
         ros_gz_sim.  gz_sim.launch.py reads this export automatically. -->
    <gazebo_ros gazebo_model_path="${prefix}/.."/>
  </export>
</package>
